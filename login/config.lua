--------------------------------------------------------------------------------
-- config.lua
--------------------------------------------------------------------------------

local conf = {
    server_port = 10010,
    http_port = 8881,
    db = "mysql",
    redis = 'redis',
}

DEBUG = true

return conf
